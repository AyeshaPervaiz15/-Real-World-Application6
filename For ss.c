#include <stdio.h>
    int main()
    {
        int n, i;
        printf("Running the values");
        printf("\nEnter a Number:  ");
        scanf("%d",&n);
        for (int (i) = 0; i < n; i++) {
            printf("\n%d", i);
        }

    return 0;
}
